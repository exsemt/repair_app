# ------------------ spree -----------------
# init DB on server
# set password
  bundle exec rake db:seed

# init examples
  bundle exec rake spree_sample:load

# ------------ deployment ------------
  cap staging deploy:check
  cap staging deploy
